import FlexCenterSection from '../../components/FlexCenterSection';
import {FC} from 'react';
import {Text} from 'react-native';

const BuyDetailScreen: FC = () => {
  return (
    <FlexCenterSection>
      <Text>Buy Detail Screen</Text>
    </FlexCenterSection>
  );
};
export default BuyDetailScreen;
